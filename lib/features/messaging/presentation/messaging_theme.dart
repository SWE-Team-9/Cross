import 'package:flutter/material.dart';

abstract class MessagingTheme {
  static const Color background = Colors.black;
  static const Color surface = Color(0xFF121212);
  static const Color surfaceAlt = Color(0xFF1A1A1A);
  static const Color border = Colors.white10;
  static const Color accent = Color(0xFFFF5500);
  static const Color accentSoft = Color(0x33FF5500);
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textMuted = Colors.white54;
}
